# MacPorts Installer addition on 2009-11-23_at_16:17:58: adding an appropriate PATH variable for use with MacPorts.
export PATH=/opt/local/bin:/opt/local/sbin:$PATH
# Finished adapting your PATH environment variable for use with MacPorts.

# MacPorts Installer addition on 2009-11-23_at_16:17:58: adding an appropriate MANPATH variable for use with MacPorts.
export MANPATH=/opt/local/share/man:$MANPATH
# Finished adapting your MANPATH environment variable for use with MacPorts.

# To determine the default text editor.
if [ "`uname`" = 'Linux' ]; then
  export EDITOR="gvim"
else
  export EDITOR="mvim -p"
fi

# My path.
export PATH=$PATH:~/bin:/usr/local/mysql-5.1.45-osx10.5-x86/bin

# Colors to show in ls -G output.
# See man ls and search for LSCOLORS to understand how this string works.
if [ "`uname`" = 'Darwin' ]; then
  export LSCOLORS="gxfxcxdxbxegedabagacad"
else
  export LS_COLORS='di=36:fi=0:ln=31:pi=5:so=5:bd=5:cd=5:or=31:mi=0:ex=35'
fi
